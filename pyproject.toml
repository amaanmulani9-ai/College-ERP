[project]
name = "college-erp"
version = "1.0.0"
requires-python = ">=3.10"

[tool.vercel]
entrypoint = "backend/college_management_system/wsgi.py"

[tool.vercel.scripts]
build = "bash scripts/vercel_build.sh"
